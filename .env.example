# ─────────────────────────────────────────────────────────────────────────────
# Crypto Radar — configuration locale (copier en `.env`, jamais committé)
# Phases 1–2 : données publiques uniquement. AUCUNE clé API n'est requise.
# ─────────────────────────────────────────────────────────────────────────────

# Mode : RADAR (observation, portefeuille virtuel) ou PAPER (trading simulé).
# LIVE n'est pas implémenté et est refusé au démarrage.
MODE=RADAR

# Source des données : "simulated" (fictives) ou "coinbase" (API publique réelle)
DATA_SOURCE=simulated

# API locale (ne pas exposer sur le réseau : 127.0.0.1 uniquement)
HOST=127.0.0.1
PORT=4000
DASHBOARD_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Univers de produits (récupéré dynamiquement, puis filtré)
QUOTE_CURRENCIES=EUR,USDC
MAX_PRODUCTS=100

# Coinbase (données publiques, sans authentification)
COINBASE_REST_BASE_URL=https://api.coinbase.com/api/v3/brokerage
COINBASE_WS_URL=wss://advanced-trade-ws.coinbase.com
COINBASE_REST_MAX_RPS=3
WS_PRODUCTS_PER_CONNECTION=25
WS_MAX_SUBSCRIBE_MSG_PER_SEC=4

# Moteur
EVAL_INTERVAL_MS=1000
SIGNAL_CONFIG_FILE=config/signal-config.json
TRADING_CONFIG_FILE=config/trading.json
PAPER_DATA_DIR=data/paper
LOG_DIR=data/logs

# Simulateur
SIM_SEED=42
SIM_TICK_MS=250

# Dashboard : Next.js lit apps/dashboard/.env.local (pas ce fichier).
# Par défaut il contacte http://127.0.0.1:4000 ; pour changer :
#   echo NEXT_PUBLIC_API_URL=http://127.0.0.1:4000 > apps/dashboard/.env.local

# ─── Compte Coinbase (optionnel, LECTURE SEULE) ─────────────────────────────
# Clé CDP créée sur https://portal.cdp.coinbase.com/ (idéalement sur un portfolio
# dédié au bot), permission **View** uniquement pour l'instant — JAMAIS Transfer :
# une clé avec Transfer est REFUSÉE au démarrage. Le bot ne passe aucun ordre.
# Option A (recommandée) : chemin du fichier JSON téléchargé, HORS du dépôt git
# COINBASE_API_KEY_FILE=/chemin/absolu/vers/cdp_api_key.json
# Option B : variables
# COINBASE_API_KEY_NAME=organizations/xxx/apiKeys/yyy
# COINBASE_API_PRIVATE_KEY=...
